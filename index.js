function getFirstSelector(selector){
  return $('selector');
}

const nestedTarget = function() {
  return document.querySelector('#nested .target');
}

const increasedRankBy = (n) => {
  let ranked-list =  parseInt(document.querySelector('.ranked-list'), 10);
}